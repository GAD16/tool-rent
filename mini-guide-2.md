# mini-guide-2.md

## Минигайд стиля: "Контрастный стройка"

## 1) Визуальная идея
Энергичный и уверенный стиль с характером стройплощадки: темная графитовая база, сигнальный желтый акцент, четкая типографика и крупные практичные блоки.

## 2) Цветовая система
Использовать CSS-переменные:

```css
:root {
  --bg: #0f172a;
  --surface: #111827;
  --surface-alt: #1f2937;

  --text: #f9fafb;
  --text-muted: #cbd5e1;

  --brand: #facc15;        /* сигнальный желтый */
  --brand-hover: #eab308;
  --brand-contrast: #111827;

  --line: #374151;
  --ok: #22c55e;
  --warning: #f59e0b;
}
```

Правила:
- Фон секций: чередование `--bg` и `--surface`.
- Акцентный цвет только один: `--brand`.
- Любые дополнительные акценты (красный/синий) использовать только в статусах, не в CTA.

## 3) Типографика
Рекомендуемые шрифты:
- Заголовки: `Roboto Condensed`, `sans-serif`
- Основной текст: `Manrope`, `sans-serif`

Шкала:
- H1: 46/54, 700
- H2: 34/42, 700
- H3: 24/32, 600
- Body L: 18/28, 500
- Body: 16/24, 400
- Small: 14/20, 400

Правила:
- Заголовки максимально конкретные, без длинных формулировок.
- На темном фоне держать повышенный межстрочный интервал для читаемости.

## 4) Сетка и отступы
- Контейнер: `max-width: 1220px`, `padding-inline: 20px`.
- Вертикальные секции (desktop): `84px`.
- Вертикальные секции (mobile): `52px`.
- Шкала отступов: 8, 12, 16, 24, 32, 48.

## 5) Hero
- Левый столбец: оффер, ключевые условия, CTA.
- Правый столбец: `hero-img.png`.
- Дополнительно: тонкие диагональные/сеточные паттерны на фоне с низкой непрозрачностью.
- CTA один: `Оставить заявку`.

## 6) Карточки и списки
- Карточки: фон `--surface-alt`, граница `1px solid var(--line)`, радиус `12px`.
- Внутренний отступ: `20px`.
- Hover: рамка слегка светлее + тень `0 10px 24px rgba(0,0,0,0.28)`.
- Маркеры списков: аккуратные желтые буллиты.

## 7) Кнопки
Основная кнопка:
- Фон: `--brand`
- Текст: `--brand-contrast`
- Высота: `50px`
- Радиус: `10px`
- Вес: `700`

Вторичная кнопка (если нужна):
- Прозрачная, с желтой обводкой и светлым текстом.

Состояния:
- `:hover` -> `--brand-hover`
- `:active` -> чуть темнее `--brand-hover`
- `:disabled` -> `opacity: 0.5`

## 8) Форма заявки
- Контейнер формы: `--surface-alt`, контрастная граница.
- Поля: темный фон, светлый текст, высота `46px`, радиус `10px`.
- Placeholder: `#94a3b8`.
- Focus: желтая рамка + мягкое свечение.
- Ошибка: рамка `#ef4444`, текст `#fca5a5`.

## 9) Блок "Как нас найти"
- Слева: адрес, ориентир, режим работы.
- Справа: `map.png` с светлой рамкой.
- Под картой: кнопки маршрута в контрастном стиле.
- Приоритет: ясность маршрута, минимум декора.

## 10) Изображения
Файлы проекта:
- `hero-img.png`
- `tools-types.png`
- `map.png`

Правила:
- На темном фоне изображения должны быть достаточно яркими.
- Если изображение слишком тусклое, добавлять легкую коррекцию `brightness(1.06) contrast(1.05)`.
- Оптимизация под web: 250-450 KB на файл для первого экрана и ключевых блоков.

## 11) Анимации
- Появление секций: `fade + translateY(14px)` за `320-480ms`.
- Hover карточек/кнопок.
- Без тяжелых эффектов и резких миганий.

## 12) Адаптив
Точки:
- `1024px` и ниже: уплотнение сетки, H1 -> 40px
- `768px` и ниже: Hero в одну колонку, кнопки и поля на 100%
- `480px` и ниже: H1 -> 32px, более плотные отступы

## 13) Принципы качества
- Контраст текста на темном фоне строго AA и выше.
- Кликабельные элементы не меньше 44x44 px.
- Первый экран должен за 3-5 секунд отвечать на вопросы: что, где, как и за сколько времени.
- CTA виден без скролла на большинстве мобильных экранов.
