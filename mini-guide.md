# mini-guide.md

## Минигайд стиля: "Индустриальный чистый"

## 1) Визуальная идея
Сдержанный, надежный и практичный визуальный язык: графитовая база, чистый фон, один сигнальный акцент для действий. Интерфейс должен выглядеть как сервис, которому можно доверить задачу "срочно нужен рабочий инструмент".

## 2) Цветовая система
Использовать CSS-переменные:

```css
:root {
  --bg: #f3f4f6;
  --surface: #ffffff;
  --surface-alt: #e5e7eb;

  --text: #111827;
  --text-muted: #4b5563;

  --brand: #f97316;        /* оранжевый акцент */
  --brand-hover: #ea580c;
  --brand-contrast: #ffffff;

  --line: #d1d5db;
  --ok: #15803d;
  --warning: #b45309;
}
```

Правила:
- Основной фон страницы: `--bg`.
- Карточки и форма: `--surface`.
- Единственный акцентный цвет для CTA и активных элементов: `--brand`.
- Не добавлять второй яркий акцент, чтобы не распылять внимание.

## 3) Типографика
Рекомендуемые шрифты:
- Заголовки: `Oswald`, `sans-serif`
- Основной текст: `Manrope`, `sans-serif`

Шкала:
- H1: 44/52, 700
- H2: 32/40, 700
- H3: 24/32, 600
- Body L: 18/28, 500
- Body: 16/24, 400
- Small: 14/20, 400

Правила:
- Все заголовки короткие, без канцелярита.
- Длина строки основного текста: до 70 символов.

## 4) Сетка и отступы
- Контейнер: `max-width: 1200px`, `padding-inline: 20px`.
- Вертикальный ритм секций (desktop): `80px`.
- Вертикальный ритм секций (mobile): `48px`.
- Базовая шкала отступов: 8, 12, 16, 24, 32, 48.

## 5) Блок Hero
- Левый столбец: заголовок, подзаголовок, три условия, CTA.
- Правый столбец: `hero-img.png`.
- Фон секции: светлый, с мягким градиентным пятном в правом верхнем углу.
- CTA только один: `Оставить заявку`.

## 6) Карточки преимуществ и категорий
- Карточка: белый фон, радиус `14px`, граница `1px solid var(--line)`.
- Внутренний отступ: `20px`.
- При hover: легкий подъем `translateY(-2px)` + тень `0 10px 24px rgba(17,24,39,0.08)`.
- Иконки: монохромные, без кислотных цветов.

## 7) Кнопки
Основная кнопка:
- Фон: `--brand`
- Текст: `--brand-contrast`
- Высота: `48px`
- Радиус: `10px`
- Вес: `600`

Состояния:
- `:hover` -> `--brand-hover`
- `:active` -> чуть темнее `--brand-hover`
- `:disabled` -> `opacity: 0.55`, `cursor: not-allowed`

## 8) Форма заявки
- Фон формы: `--surface`
- Поля: высота `46px`, граница `1px solid var(--line)`, радиус `10px`
- Focus: рамка `--brand`, мягкая внешняя тень
- Подписи полей: 14px, `--text-muted`
- Ошибки: текст `#b91c1c`, рамка поля `#ef4444`

## 9) Блок "Как нас найти"
- Слева: текст, адрес, ориентир, режим работы.
- Справа: изображение карты `map.png`.
- Под картой: 2 кнопки/ссылки:
  - `Построить маршрут`
  - `Открыть в навигаторе`
- Визуально этот блок должен быть максимально утилитарным, без декоративного перегруза.

## 10) Изображения
Файлы проекта:
- `hero-img.png` — главный визуал первого экрана
- `tools-types.png` — блок про категории инструмента
- `map.png` — блок "Как нас найти"

Правила:
- Без людей в hero.
- Сохранять четкость инструмента, избегать слишком темного кропа.
- Для ускорения загрузки подготовить web-версии (webp/jpg) до ~250-400 KB на изображение.

## 11) Анимации
Минимум и по делу:
- Появление секций при скролле: `fade + translateY(12px)` за `300-450ms`.
- Hover карточек и кнопок.
- Без сложных параллаксов и агрессивных эффектов.

## 12) Адаптив
Точки:
- `1024px` и ниже: уплотнить отступы, H1 -> 38px
- `768px` и ниже: Hero в одну колонку, картинка под текстом
- `480px` и ниже: H1 -> 30px, кнопка и поля на всю ширину

## 13) Принципы качества
- Контраст текста не ниже WCAG AA для обычного текста.
- Кликабельные элементы не меньше 44x44 px.
- Форма отправляется за 1 сценарий, без лишних шагов.
- На первом экране сразу понятны: где, что, на каких условиях и как оставить заявку.
